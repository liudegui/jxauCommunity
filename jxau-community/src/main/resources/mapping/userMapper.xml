<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liudegui.dao.IUserDao">


    <select id="selUser" resultType="com.liudegui.entity.User">
        select
            stud_id,
            stud_password,
            stud_image,
            stud_name,
            stud_gender,
            stud_class,
            stud_status,
            stud_address,
            stud_motto
        from
            user
        where
            stud_id = #{username} and stud_password=#{password}
    </select>
    <select id="selUser1info" resultType="com.liudegui.entity.User">
        select
            stud_id,
            stud_password,
            stud_image,
            stud_name,
            stud_gender,
            stud_class,
            stud_status,
            stud_address,
            stud_motto
        from
            user
        where
            stud_id=#{userId}
    </select>

</mapper>